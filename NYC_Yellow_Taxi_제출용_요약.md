# NYC Yellow Taxi End-to-End 데이터 분석

**5반 3조: 강승원 · 노은서 · 유길선 · 윤서준 · 이정현 · 최현우**

## 1. 프로젝트 개요

NYC Yellow Taxi **2026년 5월 운행 데이터 4,090,836건**을 대상으로 데이터 준비부터
시각화, 통계 검정, 머신러닝 모델링까지 End-to-End 분석을 수행하였다.

주요 목표는 다음과 같다.

- Pandas와 Polars의 로딩 결과 및 처리 특성 비교
- 구조적 결측치와 중복·이상치의 원인을 고려한 데이터 정제
- 시간·요일·거리·결제수단별 택시 운행 패턴 시각화
- 장거리·단거리 운행의 팁 비율 차이 검정
- 고액 팁 여부를 예측하는 sklearn Pipeline 구축 및 모델 저장
- 분석 결과와 품질 검증 결과의 자동 보고서 생성

## 2. 데이터 이해: 택시를 타는 순서로 컬럼 읽기

20개 컬럼을 단순히 외우기보다 **택시 이용 과정**에 맞춰 보면 각 값이 생성된 이유와
분석 시 함께 봐야 할 컬럼을 쉽게 이해할 수 있다.

| 이용 단계 | 주요 컬럼 | 의미와 분석 활용 |
|---|---|---|
| 1. 기록 수집 | `VendorID` | 운행 기록을 수집한 단말기·시스템 공급자를 구분한다. 공급자별 기록 방식 차이는 결측 패턴을 해석하는 단서가 된다. |
| 2. 승차·하차 | `tpep_pickup_datetime`, `tpep_dropoff_datetime` | 승하차 시각으로 운행시간·요일·시간대·러시아워 변수를 만들고 음수·0 이하 소요시간을 검사한다. |
| 3. 승객 | `passenger_count` | 기사가 단말기에 입력한 승객 수다. 미입력·비정상 코드가 있을 수 있어 결측 구조를 확인해야 한다. |
| 4. 출발·도착 | `PULocationID`, `DOLocationID` | 승차·하차 Zone 코드다. Zone Lookup과 결합하면 실제 지역명과 자치구 단위 분석이 가능하다. |
| 5. 요금 체계 | `RatecodeID` | 표준요금, 공항·외곽·협상요금 등 최종 요율 유형을 나타낸다. 공항 운행과 일반 운행을 구분하는 핵심 변수다. |
| 6. 이동 | `trip_distance` | 미터기에 기록된 마일 단위 운행 거리다. 소요시간과 결합해 속도를 만들고 거리·요금 관계를 분석한다. |
| 7. 결제·전송 | `payment_type`, `store_and_fwd_flag` | 결제수단과 실시간·지연 전송 여부다. 특히 현금 팁은 시스템에 기록되지 않을 수 있어 팁 분석 시 반드시 결제수단을 함께 본다. |
| 8. 영수증 | `fare_amount`부터 `total_amount`까지 | 기본요금·세금·팁·통행료·혼잡 및 공항 부가금 등 최종 결제 금액의 구성 항목이다. |

### 분석 목적별 컬럼 조합

| 분석 목적 | 함께 확인한 컬럼 |
|---|---|
| 요금 분석 | `fare_amount`, `extra`, `mta_tax`, `tip_amount`, `tolls_amount`, 각종 surcharge, `total_amount` |
| 이동 패턴 | `PULocationID`, `DOLocationID`, `trip_distance`, 파생 운행시간·속도 |
| 공항 운행 | `RatecodeID`, `Airport_fee`, 승하차 LocationID |
| 팁 분석 | `tip_amount`, `payment_type`, `record_source` |
| 시간대 분석 | 승차·하차 시각, `extra`, 파생 `is_rush_hour` |
| 혼잡 구역 | `congestion_surcharge`, `cbd_congestion_fee`, 승하차 LocationID |

이 관점은 뒤의 전처리·시각화·통계·모델 설계를 연결한다. 예를 들어 승하차 시각은
시간대별 수요와 소요시간 이상치에, 결제수단은 팁 데이터의 관측 가능 여부에,
RatecodeID와 LocationID는 공항 운행 판별에 사용된다.

> **결측치 해석 주의:** 부가요금이나 공항 관련 컬럼이 비어 있다고 해서 곧바로
> ‘해당 요금이 없었다’고 판단해 0으로 채우지 않았다. 실제 데이터에서는 특정 소스가
> 여러 컬럼을 함께 제공하지 않는 구조적 결측이 확인되었기 때문에 `record_source`로
> 구분하고 분석 목적에 맞는 분모를 사용하였다.

## 3. 주요 시각화 요약 — 정제 전 원본 데이터

결측·중복·이상치를 정제하기 **전** 원본 데이터(4,090,836행 × 20열)를 Pandas·Polars
양쪽으로 로딩해 결과가 일치함을 확인한 뒤, 그대로 시각화하여 다음을 발견하였다
(`02_visualization_original.ipynb`) 참조.

- **결측치**: 5개 컬럼(`passenger_count`, `RatecodeID`, `store_and_fwd_flag`,
  `congestion_surcharge`, `Airport_fee`)이 정확히 같은 955,371행(23.35%)에서 결측
  (동시발생 상관계수 1.00). `payment_type == 0`과 100% 일치, `VendorID == 6`은
  결측행에만·`== 7`은 비결측행에만 존재 — 무작위가 아닌 **구조적 결측**
- **데이터 품질**: `trip_distance` ≤ 0(113,031건, 2.76%), 소요시간 ≤ 0(52,063건,
  1.27%), `total_amount` 음수(14,877건, 0.36%) 등
- **극단 이상치와 상관관계 함정**: `trip_distance` 최댓값 307,491마일(왜도 386.6).
  원본 그대로면 `trip_distance`↔`fare_amount` 상관계수가 **r=0.01**(거의 무관)이지만,
  거리<50mi·요금<$200로 필터링하면 뚜렷한 양의 상관관계가 드러남 — **극단 이상치
  소수가 실제 관계를 완전히 가릴 수 있음**을 확인
- **시간대 패턴**: 17~18시 최고점(약 28만 건), 새벽 3~6시 최저(3~4만 건). 다만 오전
  7~9시는 완만한 증가 구간일 뿐 뚜렷한 출근 피크로 보기 어려워, 저녁 단일 피크에
  가까운 형태
- **결제수단**: 신용카드는 팁이 넓게 분포, 현금은 거의 0(구조적 미기록)
- **지역 편중**: 전체의 86.5%(3,537,655건)가 Manhattan 출발이나, 평균 요금은 오히려
  EWR($113.70)·JFK 포함 Queens($67.19)가 더 높음

## 4. 발견한 이슈를 바탕으로 한 처리 기준 설계

3장에서 확인한 문제를 다음과 같이 결측치 처리, 중복·이상치 처리, 파생변수 생성으로
연결하였다.

### 4.1 결측치 처리

구조적 결측(5개 컬럼, 955,371행)에 `dropna()`를 적용하면 전체의 23.35%가 삭제되고
심야·주말 데이터가 선택적으로 줄어들 수 있다. 따라서 행을 일괄 삭제하거나 임의의
값으로 채우지 않고 다음과 같이 처리하였다.

- 코드북에 없는 위장 결측값을 실제 결측값으로 변환
- 데이터 출처를 `full`과 `partial`로 구분하는 `record_source` 생성
- 분석에 필요한 컬럼이 존재하는 집단만 명확한 분모로 사용
- 모델 전처리는 sklearn Pipeline 내부에서 수행

### 4.2 중복·이상치 처리

20개 컬럼이 모두 같은 완전 중복은 없었지만, 운행 식별용 부분키를 기준으로 취소·상쇄
기록과 이중계상 기록을 발견하였다.

- 상쇄쌍 13,301쌍: 양수·음수 기록을 함께 제거
- 이중계상 16,086건: 큰 금액으로 중복 계산된 행만 제거
- 기타 판단이 어려운 14개 그룹: 임의 삭제 없이 보존

이후 기간·소요시간·거리·금액 이상치를 처리하였다. 여기에 더해, 3장에서 확인한
상관관계 함정(거리·시간을 개별로는 정상 범위라도 조합하면 물리적으로 불가능한
경우)을 잡기 위해 `speed_kmh`(속력) 기반 이상치 규칙을 추가하였다 — 개별 기준을
모두 통과하지만 200km/h를 넘는 기록을 추가로 제거하였다.

```text
원본 4,090,836행
 → 중복 유형 판정 후 4,048,148행
 → 이상치 처리 후 3,884,062행
```

최종적으로 원본의 **94.95%**를 보존하였다.

### 4.3 파생변수 생성

3장의 시간대 패턴과 결제수단별 팁 구조를 반영해 다음 파생변수를 추가하였다.

| 생성변수 | 생성 방법 | 의미와 활용 |
|---|---|---|
| `speed_kmh` | `trip_distance`를 km로 변환한 후 운행시간으로 나눔 | 운행별 평균속도. 정체 정도와 비정상 속도 운행을 확인하는 데 사용(4.2절 이상치 규칙에도 활용) |
| `is_rush_hour` | 승차 시각이 설정한 출퇴근 시간대에 포함되는지 판별 | 출퇴근 시간대와 그 외 시간대의 운행량·속도 차이를 비교 |
| `is_airport_trip` | `RatecodeID`, `Airport_fee`, 공항 관련 LocationID를 종합해 판별 | JFK·Newark 등 공항 운행과 일반 운행의 요금·거리·팁 차이를 분석 |

```python
duration_hour = duration_min / 60
speed_kmh = (trip_distance * 1.60934) / duration_hour
```

소요시간이 0 이하면 속도를 계산할 수 없어 이상치·결측으로 관리하였고, `is_airport_trip`은
`partial` 소스에서 요율·공항 컬럼 자체가 없을 수 있어 무조건 `False`가 아닌 **판단
불가(NA)** 상태를 유지하였다.

- `is_rush_hour=True` 비율: 전체 정제 데이터의 약 **35.7%**
- 출퇴근 시간대 평균속도 **15.4km/h** vs 그 외 시간대 **17.5km/h**
- `is_airport_trip=True` 비율(판단 가능 데이터 기준): 약 **9.1%**
- 공항 운행 평균 팁 비율 **21.81%** vs 일반 운행 **24.52%**

출퇴근 시간대의 평균속도가 더 낮게 나타나 `is_rush_hour`가 혼잡 상황을 어느 정도
반영함을 확인하였다. 다만 3장에서 확인했듯 실제 피크는 저녁(17~18시) 단일 피크에
가까워, 현재 경계값(7~9시·16~19시)은 저녁 위주로 재검토할 여지가 있다.

## 5. 핵심 결과

| 항목 | 결과 |
|---|---:|
| 원본 데이터 | 4,090,836행 |
| 최종 데이터 | 3,884,062행 |
| 데이터 보존율 | 94.95% |
| 완전 중복 | 0건 |
| 부분키 중복 그룹 | 29,401개 |
| 생성된 시각화 | Seaborn 7개 · Plotly 3개 |
| t-test 효과크기 | Cohen's d = -0.703(중간) |
| 모델 Accuracy | 0.7693 |
| 모델 F1-score | 0.8206 |
| 모델 ROC-AUC | 0.7877 |
| 품질 게이트 | 14개 중 14개 PASS |
| 전체 실행 시간 | 23.957초 |

## 6. 통계 분석

### 6.1 기술통계

| 변수 | 평균 | 중앙값 | 95% | 99% |
|---|---:|---:|---:|---:|
| 운행 거리(mile) | 3.51 | 1.93 | 12.60 | 19.43 |
| 기본요금(USD) | 21.45 | 16.29 | 58.30 | 78.60 |
| 팁(USD) | 3.03 | 2.36 | 10.70 | 17.49 |
| 최종금액(USD) | 30.41 | 23.85 | 78.15 | 105.08 |
| 평균속도(km/h) | 16.74 | 14.39 | 36.57 | 53.00 |

운행 거리의 평균은 중앙값의 1.82배로, 오른쪽 꼬리가 긴 분포를 보였다. 따라서 전형적인
택시 운행을 설명할 때 평균만 사용하면 소수의 장거리 운행 때문에 결과가 과장될 수 있다.

### 6.2 상관관계

- 운행 거리와 기본요금의 Pearson 상관계수: **0.867**
- 운행 거리와 최종금액의 Pearson 상관계수: **0.860**
- 기본요금과 최종금액의 Pearson 상관계수: **0.966**

`fare_amount`와 `tip_amount`는 Pearson과 Spearman 계수의 차이가 0.352로 가장 컸다.
팁이 0인 운행이 많아 순위 계산에서 동점이 발생하기 때문이다. 상관관계는 변수 간 동반
변화를 의미할 뿐, 인과관계를 증명하지는 않는다.

### 6.3 t-test: 장거리·단거리 팁 비율

카드결제 2,659,518건을 대상으로 운행 거리 5마일을 기준으로 두 집단을 비교하였다.

| 집단 | 표본 수 | 평균 팁 비율 |
|---|---:|---:|
| 장거리(5마일 이상) | 476,884 | 17.17% |
| 단거리(5마일 미만) | 2,182,634 | 25.85% |

| 평균 차이 | t 통계량 | p-value | Cohen's d |
|---:|---:|---:|---:|
| -8.69%p | -450.87 | < 2.23e-308 | -0.703 |

p-value가 0.05보다 작아 두 집단의 평균 팁 비율 차이는 통계적으로 유의하였다. 또한
Cohen's d의 절댓값이 0.703으로 중간 수준이므로, 표본 수가 커서 생긴 통계적 차이를 넘어
실질적으로도 의미 있는 차이로 해석하였다.

## 7. ML Pipeline

### 7.1 모델 데이터가 만들어지는 흐름

모델은 원본 4,090,836건을 바로 사용하지 않고, 앞선 전처리 결과를 순서대로 전달받는다.
먼저 5개 컬럼이 동일한 955,371행에서 함께 비는 구조를 확인해 행 삭제 대신
`record_source`를 만들고, 위장 결측값 1,137,013개를 `NaN`으로 변환하였다. 이후 상쇄쌍과
이중계상을 구분해 42,688행을 제거하고, 기간·소요시간·거리·금액·속도 기준으로 164,086행을
추가 제거하여 **3,884,062건**을 모델 이전 데이터로 확정하였다.

```text
원본 4,090,836건
 → 결측 행 보존 및 위장 결측 변환
 → 중복 처리 4,048,148건
 → 이상치 처리 3,884,062건
 → 카드결제 및 팁 비율 검증 2,659,518건
 → 층화 추출 500,000건
 → 학습 400,000건 / 평가 100,000건
```

정제 데이터의 카드결제는 2,660,433건이었다. `train_model()`에서는 팁 비율을 계산할 수
없거나 팁 비율이 200% 이상인 915건을 제외해 모델 모집단을 **2,659,518건**으로 정하였다.
팁 비율 25% 이상을 고액 팁으로 정의했을 때 양성 비율은 **56.14%**로 나타나 한쪽 클래스에
크게 치우치지 않았다.

### 7.2 전처리와 학습 결과

모델 입력은 수치형 10개와 범주형 6개로 구성하였다. 수치형에는 중앙값 대체와 표준화를,
범주형에는 최빈값 대체와 원-핫 인코딩을 적용하였다. 희귀 범주는
`min_frequency=0.005`로 묶어 원-핫 컬럼이 지나치게 늘어나는 것을 막았다.
`tip_amount`, `total_amount`, `payment_type`은 정답을 직접 또는 간접적으로 알려줄 수 있어
제외하였다.

전처리는 `ColumnTransformer`와 `Pipeline` 안에서 수행하여 평가 데이터의 정보가 학습
과정에 들어가지 않도록 하였다. 최종 분류기는 `HistGradientBoostingClassifier`를
사용하였다.

| Accuracy | Precision | Recall | F1-score | ROC-AUC |
|---:|---:|---:|---:|---:|
| 0.7693 | 0.7282 | 0.9399 | **0.8206** | 0.7877 |

다수 클래스 기준선 정확도 0.5614보다 0.2079 높았다. 재현율이 0.9399로 실제 고액 팁 운행을
대부분 찾았지만 정밀도는 0.7282였으므로, 후보를 넓게 찾는 데는 유용하나 확정 판정에는
추가 검토가 필요하다. 또한 요금과 소요시간처럼 운행 종료 후 확정되는 변수를 사용하므로,
현재 모델은 승차 전 예측보다 **운행 완료 후 고액 팁 패턴을 분류하는 모델**에 가깝다.
전처리를 포함한 전체 Pipeline은 `model.joblib`로 저장하였다.

## 8. 자동화·결론 및 개선 방향

### 8.1 코드 흐름과 결과의 연결

`run_pipeline.py`가 설정과 실행 옵션을 읽으면 `runner.py`가 다음 10개 단계를 등록된 순서로
실행한다. 각 단계가 반환한 DataFrame과 지표는 다음 단계와 최종 리포트에 그대로 전달된다.

```text
compare_loaders
 → analyze_missing → prepare_missing
 → deduplicate → filter_outliers
 → engineer_features → validate_features
 → visualize → statistics → model
```

| 코드 단계 | 확인한 지표 | 판단과 다음 단계에 미친 영향 |
|---|---:|---|
| `compare_loaders()` | Pandas·Polars의 shape, 결측수, 완전중복 결과 일치 | 이후 분석 결과가 로딩 도구의 차이가 아닌 데이터 자체의 특성임을 확인하였다. |
| `analyze_missing()` → `prepare_missing()` | 동시 결측 955,371행, 부분 결측 0건, `dropna()` 후 평균 거리 4.95→3.39마일 | 구조적 결측으로 판단해 행을 보존하고 `full`과 `partial` 출처로 구분하였다. |
| `deduplicate()` → `filter_outliers()` | 중복 42,688행, 이상치 164,086행 제거 | 원인별로 필요한 행만 제거해 원본의 94.95%를 남기고 음수 총액을 0건으로 만들었다. |
| `engineer_features()` → `validate_features()` → `statistics_step()` | 러시아워 35.66%, 공항 운행 9.14%, 장거리 팁 17.17%·단거리 25.85%, Cohen's d=-0.703 | 파생변수는 보조 변수로 사용하고, 거리별 팁 차이는 실질적으로 의미 있는 결과로 해석하였다. |
| `train_model()` → `evaluate()` | 모델 F1 0.8206, 차트 10개, 최종 3,884,062행 | 정확값과 허용 범위로 구성한 14개 품질 게이트를 모두 통과한 경우에만 실행을 성공으로 기록하였다. |

### 8.2 한계 및 개선 방향

| 한계 | 개선 방향 |
|---|---|
| 소요시간 제거 정책으로 `VendorID=7`의 51,750건이 전부 제외되었다. | 운행량 분석에서는 `duration_policy="flag"` 결과와 비교한다. |
| 한 달 데이터를 무작위 분할해 모델을 평가하였다. | 시간 순서 분할과 다른 월 데이터 검증을 추가한다. |
| 러시아워·공항 파생변수의 효과크기가 각각 약 -0.205, -0.212로 작았다. | 시간대 기준을 다시 조정하고 변수 중요도를 확인해 실제 기여도를 검증한다. |

> **최종 요약:** 전처리 단계마다 지표를 확인해 원인에 맞는 처리 방식을 선택했고, 최종
> 3,884,062건으로 통계 검정과 모델 학습을 수행하였다. 모델 F1-score는 0.8206이었으며,
> 전체 결과는 14개 품질 게이트를 통과한 뒤 자동 저장되었다.


### 8.3 결론 및 의견

이번 분석의 핵심은 결과값 자체보다 **전처리 근거를 지표로 확인하고 그 결과를 다음 단계에
연결한 점**이다. 결측은 단순 삭제하지 않았고, 중복은 금액 관계로 유형을 나눴으며, 거리와
시간을 결합한 `speed_kmh`로 개별 범위 검사에서 놓칠 수 있는 기록도 처리하였다. 그 결과
원본의 **94.95%인 3,884,062건**을 보존한 상태에서 통계 검정과 모델 학습을 수행하였다.

특히 구조적 결측 955,371행을 `record_source`로 구분한 것은 심야·장거리 운행이 분석에서
과도하게 빠지는 것을 막았고, 상쇄쌍과 이중계상을 구분하여 42,688행만 선택적으로 제거한
것은 정상 운행과 매출 기록을 함께 보존하는 데 영향을 주었다. 또한 기간·소요시간·거리·금액·
속도 기준을 순서대로 적용하면서 최종 데이터의 음수 총액을 0건으로 만들었다. 따라서 이후에
확인한 거리와 요금의 관계, 장거리와 단거리의 팁 비율 차이, 고액 팁 분류 성능은 서로 다른
기준으로 임의 처리한 데이터가 아니라 동일한 정제 기준을 통과한 데이터에서 나온 결과라는
점에서 의미가 있다.

정제 데이터에서는 장거리 운행의 평균 팁 비율이 17.17%로 단거리의 25.85%보다 낮았고,
Cohen's d도 -0.703으로 나타나 통계적으로 유의할 뿐 아니라 실제 차이도 확인하였다. 모델은
F1-score 0.8206과 Recall 0.9399를 기록하여 고액 팁 운행 후보를 넓게 찾는 데 활용 가능성을
보였다. 다만 현금 팁의 미기록, 2026년 5월 한 달이라는 분석 범위, 무작위 학습·평가 분할을
고려하면 이 결과를 전체 승객의 행동이나 직접적인 인과관계로 확대해서 해석해서는 안 된다.

실행이 끝나면 최종 parquet과 함께 `metrics.json`, `manifest.json`, `report.md`,
`model.joblib`을 저장한다. 또한 14개 품질 게이트로 데이터 건수, 보존율, 시각화, 통계와 모델
결과를 함께 검사한다. 이를 통해 이번 분석을 일회성 EDA로 끝내지 않고, 동일한 입력 조건과 전처리·평가 
기준을 바탕으로, 데이터 시점이 달라져도 분석 결과를 재현할 수 있도록 파이프라인을 구축하였다.
