# NYC Yellow Taxi 2026-05 — 결측치·중복 처리 기준 분석

- **대상 데이터** : `data/raw/yellow_tripdata_2026-05.parquet` (66.5 MB, 4,090,836행 × 20열)
- **작성일** : 2026-08-04 (2026-08-05 parquet 기준으로 갱신)
- **분석 도구** : pandas 2.3.3 / polars 1.43.2 (Python 3.11.15)
- **구현** : `taxi_pipeline/` 패키지 — 이 문서의 모든 기준을 단계별 모듈(`steps/missing.py`,
  `steps/duplicates.py`, `steps/outliers.py`)로 옮기고, `config/pipeline.toml`의 품질
  게이트로 기대값을 대조한다
- **목적** : "결측치는 어떻게 채우고, 중복은 어떤 키로 지울 것인가"를 관례가 아니라
  **이 데이터에서 실제로 관측된 근거**로 결정한다.

---

## 0. 결론 요약

| 항목 | 통상적인 처리 | 이 데이터에서의 결론 | 핵심 근거 |
|---|---|---|---|
| 결측치 | `dropna()` | **삭제 금지. 플래그 컬럼으로 분리** | 결측 5개 컬럼이 **100% 같은 행**에서 발생(955,371행, 23.35%). 삭제 시 특정 사업자·심야·주말 운행이 통째로 사라짐 |
| 결측 대체 | `fillna(0)` / 평균 | **금지. 결측 상태로 유지** | `congestion_surcharge` 결측행의 **61.5%는 총액 검산상 실제로 2.50이 부과됨**. 0으로 채우면 평균이 2.203 → 1.689로 왜곡 |
| 중복 | `drop_duplicates()` | **완전중복은 0건. 무의미** | 20개 컬럼 전부 같은 행 = **0건** (pandas·polars 양쪽 확인) |
| 중복 키 | 임의의 부분키 | **`(승차시각, 하차시각, PU존, DO존)` 4개 키** | 이보다 좁으면 우연 충돌, 넓히면(+거리+총액) 중복이 0이 되어 탐지 불가 |
| 중복 제거 방식 | `keep='first'` | **금지. 유형별로 다르게 처리** | `keep='first'`는 **정상 운행 29,370건을 지우고 취소 기록 14,847건을 남긴다** (완전히 거꾸로) |

**최종 파이프라인 적용 결과: 4,090,836 → 3,884,622행 (94.96% 보존)**

---

## 1. 데이터 준비 — pandas vs polars 로딩 비교

동일 parquet을 두 라이브러리로 읽어 비교했다.

**입력을 parquet으로 고정한 이유**: CSV에는 타입 정보가 없어 날짜가 문자열로 읽힌다.
그러면 §4의 소요시간 기준을 계산할 수 없고, 읽는 쪽 설정에 따라 결과가 달라진다.
parquet은 타입이 파일에 저장돼 있어 **"어떻게 읽었는가"라는 변수 자체를 없앤다.**

| 항목 | pandas 2.3.3 | polars 1.43.2 | 비고 |
|---|---|---|---|
| 로딩 시간 | 0.09~0.11초 | **0.04~0.06초** | polars 약 2배 (3회 반복 측정) |
| DataFrame 크기 | 741.5 MB (`deep=True`) | **550.1 MB** | 1.35배. polars는 문자열을 Arrow로 저장 |
| 최대 RSS (로딩+지표계산) | 2,160~2,360 MB | **1,660~1,840 MB** | 약 1.3배 |
| 타입 지정 | 불필요 | 불필요 | parquet은 타입이 파일에 있음 |
| 지연 평가 | 없음 | `scan_parquet` **0.035초** | 20개 중 **3개 컬럼만** 읽고 집계까지 완료 |

지연 평가 격차가 가장 크다. "존별 평균 요금" 하나를 구하는 데
pandas는 550MB를 전부 올려야 하지만, polars는 필요한 3개 컬럼만 읽고 0.035초에 끝낸다.

**로딩 결과 일치 여부**: shape(4,090,836 × 20), 컬럼별 결측 개수, 완전중복 건수(0건)가
**두 라이브러리에서 완전히 동일**했다. 즉 아래 분석은 도구 선택과 무관한 데이터 자체의 성질이다.

> **측정 시 주의 (파이프라인 실행에서 확인)**
> - 첫 실행은 OS 페이지 캐시가 비어 있어 pandas가 **5.98초**로 측정됐다가 재실행 시
>   0.09초가 나왔다(**54배 차이**). 먼저 재는 쪽만 디스크를 읽으므로, 비교 전에
>   파일을 한 번 통독해 캐시를 데워야 한다(`warm_page_cache()`).
> - 최대 RSS는 `ru_maxrss`가 **프로세스 누적 최댓값**이라 한 프로세스에서 순차로 재면
>   두 값이 같아진다(실제로 둘 다 2,009MB로 나왔다). 프로세스를 분리해야 한다(`--rss`).

### 같은 파일인데 컬럼 타입이 달라진다 ★

parquet의 타입을 두 라이브러리가 다르게 복원한다. 20개 중 2개가 갈렸다.

| 컬럼 | parquet 원본 | pandas | polars | 결측 |
|---|---|---|---|---:|
| `passenger_count` | `int64` | **`float64`** | `Int64` | 955,371 |
| `RatecodeID` | `int64` | **`float64`** | `Int64` | 955,371 |
| `store_and_fwd_flag` | `large_string` | `object` | `String` | 955,371 |

**pandas는 결측이 있는 정수 컬럼을 float64로 승격시킨다.** NaN을 표현할 방법이
float밖에 없기 때문이다. 그 결과:

- 승객수가 `1`이 아니라 `1.0`으로 출력된다 (실제 EDA 출력에서 그렇게 나온다)
- 정수 코드값을 `==` 로 비교할 때 부동소수 문제를 신경 써야 한다
- 8바이트 float으로 저장되어 메모리도 낭비된다

polars는 `Int64` + 별도 null 비트맵으로 정수 타입을 그대로 유지한다.
`passenger_count`, `RatecodeID`가 **결측이 있는 바로 그 컬럼들**이라는 점이 중요하다 —
이 문서가 다루는 대상이 정확히 이 타입 승격의 영향권에 있다.

**부분키 중복은 두 라이브러리가 세는 방식이 다르다** — 같은 데이터인데 2배 차이 난다.

| 호출 | 결과 | 세는 대상 |
|---|---:|---|
| polars `df.select(KEY).is_duplicated().sum()` | 58,802 | 중복 그룹의 **구성원 전부** |
| pandas `df.duplicated(subset=KEY).sum()` | 29,401 | 그룹의 **첫 행을 뺀 나머지** |

"중복 N건"이라고 보고할 때 무엇을 센 것인지 밝히지 않으면 문서끼리 숫자가 어긋난다.

**부분키 중복은 두 라이브러리가 세는 방식이 다르다** — 같은 데이터인데 2배 차이 난다.

| 호출 | 결과 | 세는 대상 |
|---|---:|---|
| polars `df.select(KEY).is_duplicated().sum()` | 58,802 | 중복 그룹의 **구성원 전부** |
| pandas `df.duplicated(subset=KEY).sum()` | 29,401 | 그룹의 **첫 행을 뺀 나머지** |

"중복 N건"이라고 보고할 때 무엇을 센 것인지 밝히지 않으면 문서끼리 숫자가 어긋난다.

### 결측 처리에 직결되는 의미론 차이 ★

| 입력 `[1.0, NaN, None]` | pandas | polars |
|---|---|---|
| `isna()` / `is_null()` | `[False, True, True]` | `[False, False, True]` |
| `is_nan()` | (구분 불가) | `[False, True, None]` |

pandas는 **"계산 실패로 생긴 NaN"과 "원본에 값이 없는 결측"을 구분하지 못한다.**
`0/0` 같은 연산 실패가 결측 통계에 섞여 들어가도 알 수 없다는 뜻이다.
polars는 둘을 분리한다. 결측 원인을 규명해야 하는 이 분석에서는 polars 쪽 의미론이 안전하며,
pandas를 쓸 경우 **파생 컬럼을 만들기 전에 결측 집계를 먼저 끝내야 한다.**

---

## 2. 결측치 분석

### 2.1 관측 — 결측은 5개 컬럼에만, 그것도 정확히 같은 개수

| 컬럼 | 결측 수 | 결측률 |
|---|---:|---:|
| `passenger_count` | 955,371 | 23.35% |
| `RatecodeID` | 955,371 | 23.35% |
| `store_and_fwd_flag` | 955,371 | 23.35% |
| `congestion_surcharge` | 955,371 | 23.35% |
| `Airport_fee` | 955,371 | 23.35% |
| 나머지 15개 컬럼 | 0 | 0.00% |

개수가 소수점까지 동일하다. 이것만으로도 랜덤 결측이 아니다.

### 2.2 결측 동시발생 검증 — 완벽하게 구조적

```
5개 컬럼 결측 패턴 조합         행 수
(모두 값 있음)              3,135,465
(모두 결측)                   955,371
(일부만 결측)                       0   ← 단 한 건도 없음
```

**5개 중 일부만 비어 있는 행이 0건**이다. 결측이 우연히 흩어진 것이라면 산술적으로 불가능한 결과다.
결측은 "값을 못 받은 것"이 아니라 **"애초에 그 필드를 제출하지 않는 다른 소스의 레코드"**다.

### 2.3 원인 규명 — 결측행은 별개의 데이터 소스

결측행 955,371건과 정상행 3,135,465건을 나눠 프로파일링했다.

| 지표 | 정상행 | 결측행 | 해석 |
|---|---:|---:|---|
| `payment_type == 0` 비율 | 0.0% | **100.0%** | 0은 TLC 코드북에 없는 값 = 결제수단 미기재 포맷 |
| `VendorID` 분포 | 1:21.8% / 2:76.6% / **7:1.65%** | 1:12.9% / 2:86.3% / **6:0.8%** | **Vendor 6은 결측행에만, Vendor 7은 정상행에만** 존재 |
| `extra` 평균 | 1.458 | **0.024** | 부가요금 항목을 사실상 채우지 않음 |
| `tip_amount` 중앙값 | 3.00 | **0.00** | 팁 정보 없음(카드결제 정보 미연동 추정) |
| 승차시각 범위 | **2008-12-31 ~ 2026-06-01** | 2026-05-01 ~ 2026-05-31 | 정상행에만 기간 이탈 오염이 있음 |
| 평균 이동거리 | 3.39 mile | 10.10 mile | 결측행 쪽에 극단 이상치 존재(최대 307,491 mile) |

`payment_type=0`이 결측행에서 **정확히 100%**, `VendorID=6`이 결측행에만 배타적으로 나타난다.
→ **결측은 특정 제출 사업자/시스템 계열의 스키마 차이에서 발생한 MNAR(비무작위 결측)**이다.

### 2.4 그래서 `dropna()`를 쓰면 안 되는 이유

결측이 랜덤(MCAR)이면 삭제해도 분포가 유지되지만, 이 데이터는 아니다.

**(1) 표본의 23.35%가 사라진다** — 4,090,836 → 3,135,465행

**(2) 특정 시간대·요일이 편향되어 사라진다**

| 구분 | 결측행 비중 |
|---|---:|
| 월요일 | 19.42% |
| 금요일 | 22.89% |
| 토요일 | 27.20% |
| **일요일** | **29.10%** |
| 오전 11시 | 16.29% |
| **새벽 4시** | **50.89%** |

새벽 4시 운행의 **절반 이상**이 결측행이다. `dropna()`는 곧 **"심야·주말 운행 데이터를 골라서
버리는 것"**이며, 시간대별 수요 분석·심야 할증 분석을 하는 순간 결론이 뒤집힌다.

**(3) 특정 사업자가 전멸한다** — `VendorID=6` 레코드 7,643건 전량 소실

**(4) 통계량이 실제로 움직인다** — 평균 이동거리 4.955 → 3.388 mile

### 2.5 `fillna(0)` / 평균 대체가 안 되는 이유 ★ 핵심 근거

"어차피 요금 컬럼이니 0으로 채우자"가 가장 흔한 처리인데, **이 데이터에서는 명백히 틀렸다.**

`total_amount`에서 `congestion_surcharge`를 뺀 나머지 항목 합을 빼서 잔차를 계산했다.
잔차 = 총액에는 반영됐지만 컬럼에는 안 적힌 금액 = **congestion_surcharge의 실제 값**이다.

| 결측행의 잔차 | 행 수 | 비율 |
|---|---:|---:|
| **정확히 2.50** | 587,435 | **61.5%** |
| 0.00 | 98,901 | 10.4% |
| 5.50 | 12,320 | 1.3% |
| 3.50 | 9,937 | 1.0% |

**결측행의 61.5%는 혼잡통행료 2.50달러가 실제로 부과된 건**이다.
(대조군: 비결측행의 `congestion_surcharge`는 2.50이 2,774,931건으로 압도적 최빈값)

→ `fillna(0)`을 적용하면 평균이 **2.203 → 1.689로 23% 과소추정**된다.
결측은 "0원"이 아니라 **"부과됐지만 이 소스가 기록하지 않은 값"**이다.

`passenger_count`도 마찬가지다. 중앙값(1.0)으로 채우면 "1명 승차" 비중이
실제 82.2% → **86.4%**로 부풀려진다. 23%를 한 값으로 채우는 이상 분포 왜곡은 불가피하다.

### 2.6 위장 결측(sentinel) 점검

`NaN`이 아니어서 `isna()`에 안 잡히지만 실질적으로 결측인 값들:

| 값 | 건수 | 의미 |
|---|---:|---|
| `payment_type == 0` | 955,371 | 코드북에 없음 → 결측과 동일 |
| `RatecodeID == 99` | 140,897 | TLC 정의상 "Unknown" |
| `passenger_count == 0` | 12,533 | 0명 탑승은 물리적으로 불가 |
| `PULocationID in (264, 265)` | 6,486 | 264=Unknown, 265=구역 외 |
| `DOLocationID in (264, 265)` | 21,726 | 〃 |
| `store_and_fwd_flag`의 `'Y'` | 3,597 | 통신 두절로 지연 전송된 기록 |

`isna()`만 믿으면 이 **약 114만 건의 실질 결측을 놓친다.**

### 2.7 권장 결측 처리 기준

| 기준 | 처리 | 근거 |
|---|---|---|
| **① 5개 컬럼 동시 결측 (955,371행)** | **행 삭제 금지.** `record_source` 플래그 컬럼을 만들어 `"partial"` / `"full"`로 구분 | 구조적 MNAR. 삭제 시 심야·주말·특정 사업자 편향(§2.4) |
| **② 결측값 채우기** | **채우지 않음.** 분석 시 해당 컬럼을 쓰는 지표는 `record_source == "full"` 부분집합에서만 계산 | `fillna(0)`은 61.5%가 2.50인 값을 0으로 왜곡(§2.5) |
| **③ 요금 총액 분석** | **전체 사용 가능** | `total_amount`, `fare_amount`는 결측 0건이며 총액에 부과분이 이미 반영돼 있음 |
| **④ 승객수·요율 분석** | **`record_source == "full"` 한정** | 해당 컬럼 자체가 없는 소스를 섞으면 분모가 오염됨 |
| **⑤ sentinel 값** | `NaN`으로 **명시 변환** 후 ①과 동일 취급 | `isna()`가 못 잡는 114만 건(§2.6) |

```python
SENTINELS = {
    "payment_type": [0],
    "RatecodeID": [99],
    "passenger_count": [0],
    "PULocationID": [264, 265],
    "DOLocationID": [264, 265],
}
for col, bad in SENTINELS.items():
    df[col] = df[col].replace(bad, pd.NA)

# 삭제가 아니라 '분리'
df["record_source"] = np.where(df["congestion_surcharge"].isna(), "partial", "full")
```

---

## 3. 중복 분석

### 3.1 완전중복은 0건 — `drop_duplicates()`는 아무 일도 하지 않는다

| 방법 | 결과 |
|---|---|
| pandas `df.duplicated().sum()` (20개 컬럼) | **0건** |
| polars `len(df) - df.n_unique()` | **0건** |

두 라이브러리 모두 0을 반환했다. **`drop_duplicates()`를 그냥 호출하면 0행이 지워지고
"중복 없음"이라고 결론짓게 되지만, 실제로는 아래처럼 중복이 존재한다.**

### 3.2 어떤 키를 쓸 것인가 — 키 폭을 넓혀가며 관측

| 키 | 컬럼 수 | 중복 건수 | 비율 |
|---|---:|---:|---:|
| A. 승차시각 + 하차시각 | 2 | 33,925 | 0.829% |
| **B. A + PU존 + DO존** | **4** | **29,401** | **0.719%** |
| C. B + 거리 + 총액 | 6 | **0** | 0.000% |
| D. C + VendorID | 7 | 0 | 0.000% |
| E. D + 요금 세부항목 | 11 | 0 | 0.000% |

**C에서 0으로 떨어진다** — 즉 중복 후보들은 **총액이나 거리가 서로 다르다.**
키를 넓게 잡으면(C~E) 중복을 아예 탐지하지 못하고, 좁게 잡으면(A) 우연 충돌이 섞인다.
**판정 경계는 B다.**

### 3.3 B키 중복이 우연인지 검증 (귀무모형)

409만 건이 44,640분에 몰려 있으니 초 단위 시각은 우연히 겹칠 수 있다.
이를 배제하기 위해, **실제 중복 구조만 파괴하고 주변 분포는 보존한 가상 데이터**를 만들어 비교했다.

> 귀무모형: 승차시각은 그대로 두고 하차시각 = 승차시각 + (소요시간 분포에서 무작위 재추출),
> PU/DO 존은 컬럼 자체를 무작위 셔플. (seed=42)

| 키 | 관측 중복 | 귀무모형 기대 | 관측/기대 |
|---|---:|---:|---:|
| A. 시각 2개 | 33,925 | 2,614 | **13.0배** |
| **B. 시각 2개 + 존 2개** | **29,401** | **2** | **14,700배** |

존 집중도(`Σp²`)는 PU 0.0216 / DO 0.0183으로, 259개 존 중 상위 5개가 19.8%를 차지할 만큼
편중돼 있는데도 **우연 기대치는 단 2건**이다.
→ **B키 중복 29,401건은 우연이 아니라 데이터 생성 과정의 산물이다.** (통계적으로 명백)

### 3.4 B키 중복군의 정체 — 3가지 유형으로 완전 분해

중복군은 **58,802행 / 29,401그룹, 전부 정확히 2행짜리 그룹**이었다. 유형별로 분류하면:

| 유형 | 그룹 수 | 비율 | 특징 |
|---|---:|---:|---|
| **① 상쇄쌍 (취소·환불)** | 13,301 | 45.2% | 두 행의 `total_amount` 합이 정확히 0. 부호만 반대 |
| **② 이중계상 (총액 2배)** | 16,086 | 54.7% | 20개 컬럼 중 `total_amount`만 정확히 2배. 나머지 전부 동일 |
| ③ 기타 | 14 | 0.05% | 거리·팁이 미세하게 다름 |

**29,401그룹 중 29,387그룹(99.95%)이 ①·② 두 유형으로 설명된다.**

**① 상쇄쌍 실례** — 같은 시각·같은 존·같은 거리, 금액만 부호 반대:

```
tpep_pickup_datetime  tpep_dropoff_datetime  PU   DO   trip_distance  fare_amount  total_amount  payment_type
2026-05-01 00:02:26   2026-05-01 00:12:49    170  113  1.88           -12.1        -17.85        4
2026-05-01 00:02:26   2026-05-01 00:12:49    170  113  1.88            12.1         17.85        4
```

보강 근거: 음수 `total_amount` 14,877건 중 **13,301건(89.4%)에 정확히 반대 금액의 짝**이 있고,
음수 행의 **60.8%가 `payment_type=4`(Dispute, 분쟁)**다.
→ **운행이 취소·환불되어 장부상 상쇄된 쌍**이 확실하다.

**② 이중계상 실례** — 총액만 정확히 2배:

```
              A행     B행
trip_distance  5.70    5.70   (동일)
fare_amount   27.73   27.73   (동일)
tip/tolls/surcharge 전부 동일
total_amount  41.48   82.96   ← 정확히 2배
payment_type      0       0   (양쪽 다 결측 포맷)
```

**16,086쌍 전부 양쪽이 `payment_type=0`**, 즉 §2.3의 결측 소스에서만 발생한다.
어느 쪽이 옳은지는 동일 포맷의 **단독행 기준선**과 비교해 판정했다:

| 지표 (`total_amount / fare_amount`) | 값 |
|---|---:|
| 동일 포맷 단독행 중앙값 | **1.231** (사분위 1.146~1.360) |
| 2배 쌍 — 작은 쪽 | **1.393** |
| 2배 쌍 — 큰 쪽 | **2.785** |

작은 쪽도 기준선 상단(1.360)을 약간 넘지만, 큰 쪽(2.785)은 정상 분포에서 완전히 벗어난다.
→ **큰 쪽이 이중 계상된 행**이다.

### 3.5 `drop_duplicates(keep='first')`의 함정 ★

B키를 찾아냈으니 `df.drop_duplicates(subset=B)`를 쓰고 싶어지지만, **결과가 정반대로 나온다.**

| `keep='first'` 적용 결과 | 건수 |
|---|---:|
| 제거된 행 중 **양수** `total_amount` | **29,370** |
| 제거된 행 중 음수 `total_amount` | 30 |
| 처리 후 남은 음수 행 | **14,847** (원본 14,877 중) |

파일에 저장된 행 순서상 상쇄쌍은 **취소 행(음수)이 먼저** 오는 경우가 많다.
따라서 `keep='first'`는 **실제 운행 기록을 지우고 취소 기록만 남긴다.**
매출 집계를 하면 그 구간이 통째로 음수가 된다. **"중복 제거"라는 이름의 데이터 파괴**다.

### 3.6 권장 중복 처리 기준

| 유형 | 판정 조건 | 처리 | 영향 |
|---|---|---|---|
| **완전중복** | 20개 컬럼 전부 일치 | 해당 없음(0건) | — |
| **① 상쇄쌍** | B키 그룹의 `total_amount` 합 ≈ 0 (±0.01) | **양쪽 모두 제거** — 취소된 운행은 발생하지 않은 운행 | -26,602행 |
| **② 이중계상** | B키 그룹에서 `total_amount` 비율이 2.0 (±0.01) | **큰 쪽만 제거**, 작은 쪽 유지 | -16,086행 |
| **③ 기타 (14건)** | 위 어디에도 해당 없음 | **유지** — 서로 다른 운행일 가능성이 높고 규모가 무시 가능 | 0행 |

**한 줄 요약: `keep='first'`가 아니라 "그룹의 금액 관계를 보고 어느 행을 지울지 결정"해야 한다.**

부수 효과로 음수 `total_amount`가 **14,877 → 1,576건(-89.4%)**으로 줄어든다.
이상치 필터로 음수를 지우는 것보다 **원인(취소쌍)을 제거하는 쪽이 정확**하다.

---

## 4. 함께 적용할 이상치 기준

중복·결측과 분리해 판단해야 하는 항목들이다.

| 항목 | 기준 | 건수 | 근거 |
|---|---|---:|---|
| 기간 이탈 | 승차시각이 2026-05 밖 | 14 | 2008년 1건, 2009년 1건 등 명백한 오기록. **하차가 6월로 넘어간 785건은 정상 심야운행이므로 승차시각으로만 판정** |
| 소요시간 | ≤ 0초 또는 > 6시간 | 53,006 | 소요시간 0 이하 52,063건은 물리적으로 불가. 상위 99.9%가 7,750초(2.15시간)이므로 6시간은 충분히 관대한 상한(최댓값은 166시간) |
| 이동거리 | = 0 또는 > 100 mile | 108,631 | 거리 0이 113,031건. 최댓값 307,491 mile은 지구 12바퀴 |
| 금액 | `total_amount ≤ 0` 또는 `fare_amount < 0` | 1,875 | 상쇄쌍 제거 후 남은 잔여 음수 |

### ★ 소요시간 기준의 부작용 — 사업자 하나가 통째로 사라진다 (파이프라인 실행 중 발견)

`taxi_pipeline/steps/outliers.py`에 넣은 자동 점검(필터 전후 `VendorID` 구성 비교)에서
잡힌 문제다.

| 관측 | 값 |
|---|---:|
| `VendorID=7` 원본 건수 | 51,750 |
| 그 중 소요시간 ≤ 0인 비율 | **100.0%** |
| 전체 소요시간 ≤ 0 (52,063건) 중 Vendor 7의 비중 | **99.4%** |
| 소요시간 필터 적용 후 남는 Vendor 7 | **0건** |

**소요시간 이상 52,063건은 "무작위 오류"가 아니라 사실상 `VendorID=7` 한 사업자의
기록 방식이다.** 이 사업자는 하차시각을 승차시각과 같거나 이전으로 기록한다.
즉 §2.4에서 `dropna()`를 거부한 것과 **똑같은 종류의 편향**이 이 필터에 있다 —
값이 이상한 게 아니라 **소스가 다른 것**이다.

이번 파이프라인은 문서에 확정한 기준을 그대로 적용해 Vendor 7을 제거했다(51,750건, 전체의 1.3%).
다만 **분석 목적에 따라 판단이 달라진다**:

- **소요시간·속도 분석** → 제거가 맞다. 애초에 계산이 불가능한 레코드다.
- **운행량·요금·존 분석** → 제거하면 사업자 하나가 통째로 빠진다.
  `duration_valid` 플래그를 두고 **소요시간을 쓰는 지표에서만 제외**하는 편이 안전하다.

```python
# 목적이 소요시간 분석이 아니라면 이렇게 바꾼다
df["duration_valid"] = dur.between(DUR_MIN_SEC, DUR_MAX_SEC)
# 삭제는 하지 않고, 소요시간 기반 집계에서만 df[df["duration_valid"]] 사용
```

> **주의**: 거리 0(113,031건) 중 1,137건은 소요시간도 0이다. 나머지는 "미터기는 켰지만
> 실제 이동이 없었던 건"으로, 취소·노쇼일 수 있어 별도 분석 가치가 있다.
> 무조건 버리기보다 **플래그를 남기고 제외**하는 편이 안전하다.

---

## 5. 최종 파이프라인과 적용 결과

### 단계별 행 수 변화

| 단계 | 처리 | 남은 행 | 제거 |
|---|---|---:|---:|
| 0 | 원본 | 4,090,836 | — |
| ① | 취소·환불 상쇄쌍 제거 (양쪽) | 4,064,234 | -26,602 |
| ② | 이중계상 행(2배) 제거 | 4,048,148 | -16,086 |
| ③ | 기간 이탈 제거 | 4,048,134 | -14 |
| ④ | 소요시간 이상 제거 | 3,995,128 | -53,006 |
| ⑤ | 거리 이상 제거 | 3,886,497 | -108,631 |
| ⑥ | 잔여 금액 이상 제거 | **3,884,622** | -1,875 |

**보존율 94.96%.** 결측행은 삭제하지 않았으므로 최종 데이터에도 22.2%(863,163행)가
`record_source="partial"`로 남아 있다.

### 통계량 변화

| 지표 | 원본 | 정제 후 | 비고 |
|---|---:|---:|---|
| 행 수 | 4,090,836 | 3,884,622 | 94.96% 보존 |
| 평균 이동거리 | 4.955 | **3.517** | 극단 이상치 제거 효과 |
| 중앙 이동거리 | 1.87 | 1.93 | 거리 0 제거로 소폭 상승 |
| 평균 요금 | 21.51 | 21.45 | 거의 불변 = **과도한 삭제가 아님** |
| 평균 총액 | 30.49 | 30.41 | 〃 |
| 음수 총액 | 14,877 | 0 | |

평균 거리는 크게 교정되면서 평균 요금은 거의 그대로다.
**"왜곡만 제거하고 정보는 보존했다"**는 증거다.

### 무성의한 처리와의 대조

| 처리 | 결과 | 문제 |
|---|---|---|
| `df.dropna()` | 3,135,465행 (76.65%) | 심야·주말 편향, Vendor 6 전멸 |
| `df.drop_duplicates(subset=B)` | 4,061,435행 | **음수 total 14,847건 잔존** (정상 운행을 대신 삭제) |
| `df.fillna(0)` | — | `congestion_surcharge` 평균 2.203 → 1.689 |
| **권장 파이프라인** | **3,884,622행 (94.96%)** | 음수 0건, 결측은 플래그로 보존 |

### 구현 코드

```python
import numpy as np
import pandas as pd

DATE_COLS = ["tpep_pickup_datetime", "tpep_dropoff_datetime"]
PU, DO = DATE_COLS
KEY = [PU, DO, "PULocationID", "DOLocationID"]

# parquet이라 parse_dates/dtype 지정이 필요 없다 — 날짜가 datetime64로 복원된다
df = pd.read_parquet("data/raw/yellow_tripdata_2026-05.parquet")

# ---- 1. 결측: 삭제가 아니라 소스 구분 ----------------------------------
df["record_source"] = np.where(df["congestion_surcharge"].isna(), "partial", "full")

# ---- 2. 중복: 그룹의 금액 관계로 유형을 판정한 뒤 선택적으로 제거 --------
dup = df[df.duplicated(subset=KEY, keep=False)].copy()
dup["gid"] = dup.groupby(KEY).ngroup()
dup = dup.sort_values(["gid", "total_amount"])          # 그룹 내 (작은값, 큰값)
lo_idx = dup.groupby("gid").head(1).index
hi_idx = dup.groupby("gid").tail(1).index

pair_sum = df.loc[lo_idx, "total_amount"].values + df.loc[hi_idx, "total_amount"].values
pair_ratio = df.loc[hi_idx, "total_amount"].values / np.where(
    df.loc[lo_idx, "total_amount"].values == 0, np.nan, df.loc[lo_idx, "total_amount"].values)

is_void = np.abs(pair_sum) < 0.01                        # ① 취소·환불 상쇄쌍
is_double = ~is_void & (pair_ratio > 1.99) & (pair_ratio < 2.01)   # ② 이중계상

drop_idx = lo_idx[is_void].union(hi_idx[is_void])        # 상쇄쌍은 양쪽 제거
drop_idx = drop_idx.union(hi_idx[is_double])             # 이중계상은 큰 쪽만
df = df.drop(index=drop_idx)

# ---- 3. 이상치 ---------------------------------------------------------
month = pd.Period("2026-05")
dur = (df[DO] - df[PU]).dt.total_seconds()
df = df[
    df[PU].between(month.start_time, month.end_time)     # 승차시각 기준(하차는 6월 허용)
    & dur.between(1, 6 * 3600)                           # ★ VendorID=7 전량 제거됨(§4 참고)
    & df["trip_distance"].between(0.01, 100)
    & (df["total_amount"] > 0) & (df["fare_amount"] >= 0)
]
```

> 실행 가능한 전체 구현은 **`taxi_pipeline/steps/`**에 있고 `python run_pipeline.py`로 돌린다.
> 각 임계값 위에 이 문서의 몇 절을 근거로 삼았는지 주석으로 달아 두었고,
> 마지막 단계에서 위 표의 기대값(행 수·그룹 수 등 7개 항목)과 실측을 대조한다.
> `yellow_tripdata_2026-05.parquet`으로 실행해 **7개 항목 전부 일치**를 확인했다.

---

## 6. 판단이 갈릴 수 있는 지점 (한계)

이 문서의 결론 중 **데이터만으로는 확정할 수 없어 가정을 세운 부분**을 명시한다.

1. **이중계상 쌍에서 "작은 쪽이 옳다"** — 동일 포맷 단독행의 `total/fare` 중앙값 1.231과
   비교해 판정했으나, 작은 쪽(1.393)도 사분위 상단(1.360)을 약간 넘는다.
   TLC 요금 산정식으로 재계산해 검증하는 것이 더 확실하다.
2. **상쇄쌍을 양쪽 다 지우는 것** — "취소된 운행은 발생하지 않은 것"이라는 전제다.
   **운행은 실제로 있었고 결제만 취소된 경우**라면 운행량 분석에서는 남겨야 한다.
   매출 분석이면 제거, 수요·교통량 분석이면 양수 행만 남기는 편이 낫다.
3. **소요시간 6시간 상한** — 99.9 분위(2.15시간)를 크게 웃도는 관대한 값으로 잡았다.
   장거리 공항 운행을 살리려는 선택이며, 도심 단거리 분석이면 2시간으로 좁혀도 무방하다.
4. **결측행을 남기는 선택** — 분석 목적이 `passenger_count`나 `RatecodeID`에 **직접**
   의존한다면 결국 `record_source == "full"`만 쓰게 되어 실질적으로 `dropna()`와 같아진다.
   차이는 **그 사실을 알고 하느냐**에 있다. 이 경우 결과에 "전체가 아닌 76.65% 표본"임을
   반드시 명시해야 한다.
5. **소요시간 필터가 `VendorID=7`을 전멸시키는 것** (§4 참고) — 51,750건 전량이
   소요시간 ≤ 0이라 필터에 걸린다. 이 문서는 기준을 그대로 적용했지만,
   **운행량·요금·존 분석이 목적이라면 `duration_valid` 플래그로 바꿔야 한다.**
   결측 처리에서 `dropna()`를 거부한 논리를 이상치 필터에도 일관되게 적용하면
   "제거"가 아니라 "표시"가 맞다. 이 문서에서 가장 재검토가 필요한 항목이다.
6. **`store_and_fwd_flag='Y'` 3,597건** — 통신 두절로 지연 전송된 기록이라 좌표·시각
   정확도가 낮을 수 있으나, 규모가 작아 이번 기준에는 포함하지 않았다.
